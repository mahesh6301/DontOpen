import ramdom
import time
import webbrowser

while true:
    site = random.choice(['instagram.com', 'youtube.com'])
    source = 'https://{}.format(site)
    webbrowser.open(source)
    time.sleep(3)
